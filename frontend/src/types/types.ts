export type Match = {
  title: string,
  id: string,
  hasPassword: boolean,
  hostUserId: number,
}

export type MatchJSON = {
  title: string,
  id: string,
  has_password: boolean,
  host_user_id: number,
}

